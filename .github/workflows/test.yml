name: test

on:
  workflow_dispatch:

jobs:
  test:
    name: script test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: test script
        run: test.sh
